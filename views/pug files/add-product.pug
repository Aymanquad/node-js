extends layout/main-layout.pug 

block styles 
  link(rel="stylesheet", href="/css files/add-product.css")
  title #{pgTitle}



block content 
  main
      form.myform(action="/product", method="POST")
        div.form-stuff
          label(for="title") Title
          input.form-input(type="text", name="title")
        button(type="submit") Send Product

